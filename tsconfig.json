{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",  // Needed for path resolution
    "paths": {
      "@/*": ["src/*"]  // Alias @/ to src/
    }
  }
}
